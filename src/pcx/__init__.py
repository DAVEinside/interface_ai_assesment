"""Progressive crystallization for UI automation.

An LLM discovers a flow on a live application surface; the successful run is
compiled into a typed, versioned capability artifact; the artifact is replayed
deterministically with no model in the decision loop; and the capability is
promoted toward determinism (or demoted away from it) on accumulated evidence.
"""

__version__ = "0.1.0"
